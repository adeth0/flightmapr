// ─────────────────────────────────────────────────────────
//  ThemeToggle — DEPRECATED (kept as an empty stub).
//
//  The standalone light/dark pill has been removed. The UI
//  theme now follows the map's day/night state automatically
//  via `syncThemeWithMap(isNight)` in services/themeService.js.
//  Any remaining imports render null so builds stay green.
// ─────────────────────────────────────────────────────────

export function ThemeToggle() { return null; }
export default ThemeToggle;
